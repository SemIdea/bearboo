---
type: evolution
description: 'Evolution cycle report: cycle 002 — 2026-09-16 — instance migration 3.5.0-rc.1 → 3.30.0 (OKF split, gate tooling, namespace) plus two staleness fixes.'
mechanism: evolve
load: cold
---

# Evolution cycle 002 — 2026-09-16

> Full `/afm:evolve` cycle (explicit invocation, "run everything"). Target: this repo's `docs/` — the materialized instance. The plugin (`framework-template/`) was only read; the boundary post-condition is green (cache dir, no file touched after `T0`, checked with `find -newermt`).

**Cycle trigger:** explicit invocation by the owner.
**New signal since the last cycle:** the last cycle with `mechanism: evolve` was 2026-08-22 (`001`), 25 days ago — outside the 14-day cooldown. The static deltas since then: A3 drift plus the two § 3 triggers on a nonexistent path, both already surfaced at SessionStart and corroborated by the à-la-carte `reflect` of today (`2026-09-16-001`, R01).

---

## Vital signs

| Signal | Before | After (measured after apply) |
| --- | --- | --- |
| Dead refs (A1) | 3 live-refs flagged by health (`domainErrorToTRPCError.ts`, `domainErrorTransport.ts`, `shared/error/domainError.ts`) | 0 live refs; 16 historical citations declared as ratchet debt in `docs/tools/gate/ratchet.tsv` |
| `[TBD]`/`[NEEDS CLARIFICATION:]` above threshold (A2) | 37 markers written `[A DEFINIR]` — invisible to the A2 counter; age 78d, under the 90d default | 37 markers normalized to `[TBD]` (live docs); none above threshold |
| Version drift (A3) | instance `3.5.0-rc.1` < plugin `3.30.0` | instance `3.30.0` (pinned after applying every approved delta) |
| Broken ADR chain (A4) | 0 | 0 |
| Non-parseable § 3 triggers (A5) | 2 (rule 19 target `docs/.afm-log-failures/` absent; vacuous-pipe check on rule 7) | 0 — rule 19 body refreshed (tolerates the absent dir); § 3 is now an index of files |
| Inflation — core docs above threshold (A6) | `ust.md` 727 / `roadmap.md` 770 lines, no near-duplicate, no orphan | unchanged; high `wc` alone is explicitly not a finding |
| Cross-doc contradiction (A7) | 0 literal conflicting normative pairs | 0 — `ach.md:235` was staleness, not a competing norm (D05) |

---

## Findings (φ signature)

| ID | Symptom (evidence) | Causal status | Mechanism | Severity | Addressable? |
| --- | --- | --- | --- | --- | --- |
| D01 | A3 drift: `docs/.afm-version` = `3.5.0-rc.1`, plugin `VERSION` = `3.30.0`; 23 consumer-migration blocks in range (structural OKF 3.9.1, gate tooling 3.20/3.21, `prd` § 11.1, inalienable rule, `[A DEFINIR]`→`[TBD]`). | Yes — the instance was bootstrapped at 3.5.0-rc.1 and never migrated. | Version drift, not a miswritten rule. | HIGH | Yes → A3, resolved by the `update` migration |
| D02 | A1: `docs/gotchas.md:143` cited `src/server/http/domainErrorToTRPCError.ts`; the entry's prose used the pre-ADR-0020 names (`DomainError`, `withDomainErrors`). ADR-0020 carries the rename table; `appErrorToTRPCError.ts` exists. | Yes — the doc lagged a landed rename. | Obsolete reference (restorable). | LOW | Yes → A01 |
| D03 | A5b: the plugin marks `PLUGIN-21` (code and tests in English) INALIENABLE; the instance's § 3 had no equivalent rule. | Yes — the floor rule was never materialized here. | Missing materialization, subordinate to the A3 range. | CRITICAL (per `diagnose` A5b) | Yes → inside the A3 umbrella |
| D04 | A5: rule 19's verification targeted `docs/.afm-log-failures/`, absent until the first recorded failure; the command errored instead of returning 0/1. | Yes — the rule text predates the plugin's graceful guard. | Stale rule text, subordinate to the A3 range. | HIGH | Yes → inside the A3 umbrella |
| D05 | Staleness: `docs/ach.md:235` stated in the present that "every existing domain throws `TRPCError` directly (debt tracked in `afm.md § 3.1`)" — false since `022-error-registry` (2026-07-27); § 3.1 no longer tracks rule 15. | Yes — historical note in the present tense. | Stale assertion (the paragraph is an epoch record). | LOW | Yes → B02 |
| D06 | Staleness: `docs/roadmap.md:15/392` said the FTS dictionary is `portuguese`; the migration uses `english`. Line 523 cited `src/server/schema/*.schema.ts`; the schemas live under `features/*/schema.ts`. | Yes — roadmap lagged features 016/027 and the folder refactor. | Stale assertions. | LOW | Yes → B03 |

### Observations (non-addressable findings — no proposal)

- **A2 under threshold:** the markers date from 2026-06-30 (78 days) and the default threshold is 90; they were normalized, not resolved. Their content stays the owner's stance (`#4`) — age is a signal to ask, never to invent.
- **Reflect — empty behavioral axis.** No doc-friction event was lived in this session (the session's friction was host tooling — installing the AFM skills into opencode — which is not this instance's docs). A valid empty result, not a skipped axis.
- **Guardrail axis — empty.** No defect was fixed by hand twice, and `docs/.afm-log-failures/` holds no `sig=`.
- **Rule 30–36 (project namespace) were preserved verbatim.** The 3.20 rename covered the inherited PLUGIN-* rules only; the instance's own rules keep their numbers (the namespaces do not collide).
- **Historical record deliberately not rewritten:** `[A DEFINIR]` inside `docs/evolution/`, `docs/features/`, and the append-only ledgers stays as written (ADR-0021 / rule 18). Only live docs were normalized.
- **Opt-ins reported, not applied:** organisation policy through `AFM_GLOBAL_DIR` (3.10.0); the optional `cadence:` line in `docs/_focus.md`; `policy_blocking` was never present.

---

## Evolution package

### Bucket (a) — auto-applicable (restorative, non-semantic)

| ID | Surface | Edit | Mechanical proof | Gate decision |
| --- | --- | --- | --- | --- |
| A01 | `docs/gotchas.md` (tRPC entry: Behavior, Solution, Ref) | `DomainError` → `AppError`, `withDomainErrors` → `withAppErrors`, choke-point path → `src/server/http/appErrorToTRPCError.ts` | ADR-0020 rename table; file exists; `createRouter.ts` mounts `withAppErrors` | approved (listed at the gate) |

### Bucket (b) — load-bearing (earned gate questions)

| ID | Targeted pattern | Edited surface | Expected effect | Regression risk / what to preserve | What breaks if this is wrong | Flip-flop? | Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| B01 | D01/D03/D04 — version drift | `docs/rules/**` (new), `docs/gotchas/**` (new), `docs/tools/gate/**` (new), `docs/index.md` + `.afm-index.tsv`, `docs/evolution/2026-08-22-001.md` (frontmatter), `docs/afm.md` § 3, `docs/prd.md` § 11.1, `docs/rubrics/{error-classification,when-to-create-dsl,negative-filters}.md`, sessions frontmatter `kind:`→`type:`, `docs/.afm-version` | Instance at `3.30.0`: OKF split (10 gotchas, 22+7 rules), gate tooling + baseline, `[TBD]` markers, inalienable rule, prd language field | Instance rules 30–36 intact; ADRs/features/sessions content untouched (only session frontmatter key); `[TBD]` content preserved; scripts count before/after | A broken parse surface (hooks) or a lost entry — mitigated by the migration's count check, the index check, and the hook re-parse post-condition | — | approved |
| B02 | D05 — stale present-tense claim | `docs/ach.md:235` | The paragraph reads as history: the debt existed then and was closed in `022-error-registry` | Paragraph structure, the AppError description, the media references | Nothing normative changes; the note becomes accurate | — | approved |
| B03 | D06 — stale facts | `docs/roadmap.md:15,392,523` | Roadmap matches the shipped migration (`english`) and the current schema location | Phase statuses, checklist history | Nothing normative changes | — | approved |

### Bucket (c) — cosmetic/deferrable

- The `README.md` line in `roadmap.md` flagged by today's à-la-carte `reflect` (handoff) was **not** verified in this cycle — left for the next one.
- The `CLAUDE.md` bridge still quotes the adoption version `v3.1.0-rc.6` (historical note; `.afm-version` is the live pin).
- Today's `2026-09-16-001.md` report carries no `type:` frontmatter (the OKF field arrived with 3.20); `PLUGIN-39` could not decide on it, so no edit was made.

### Overflow staged (above the cap of 8 items/cycle)

- None — the package carried 3 (b) items.

---

## ADRs created in this cycle

- None.

---

## Attempt memory (anti-oscillation)

### Refused in this cycle

- None.

### Approved-then-reverted (by mechanical post-condition)

- None.

### Note for the next cycle

- The 2026-08-22 cycle migrated `3.1.0-rc.6` → `3.5.0-rc.1`; this one migrated `3.5.0-rc.1` → `3.30.0`. Do not re-propose any delta now materialized — `retrieval-before-create` will find the files.

---

## Mechanical post-conditions

- [x] Re-extraction by the hooks over the touched docs kept the parseable format: `afm-session-start.sh` emits valid JSON over the migrated `docs/`; `afm-pre-edit.sh` injects `docs/rules/*` entries for matching paths (`prisma/schema.prisma`, `src/server/createRouter.ts` exercised).
- [x] The plugin was never written to: the cache dir is not a git repo, so the `find -newermt @T0` branch ran (T0=1789602515) and returned empty. (No rollback needed; no CRITICAL.)
- [x] `afm-index.sh --check-index` returns 0 (indexes materialized).
- [x] `docs/tools/gate/guardrails.sh --baseline` recorded `PLUGIN-37` (1) and `PLUGIN-40` (16) as pre-existing debt; `PLUGIN-13/21/35/36/38` clean at 0; `PLUGIN-39` undecidable. Growth now fails.

---

## Suggested promotion

- **PLUGIN-35/36/38/39 quote plugin-repo paths that no consumer materializes** (`test/rules/*.sh`, `ops/restructure.md`, `tools/afm-index.sh`). In a consumer, `PLUGIN-40` flags the rule text that the same release materialized — measured here: 4 of the 16 baseline rows. Candidate fix: rephrase to relative/plugin-qualified references.
- **Host adaptation for opencode:** the AFM plugin has no adapter there — skills use `${CLAUDE_SKILL_DIR}`, the TUI hides skill-source commands from `/` autocomplete, and the hooks do not run. A generalizable install path needs a `promote` item.

Both are **record-only** suggestions (`/afm:promote` has its own gate; the plugin boundary is absolute).
