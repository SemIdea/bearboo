# Evolution #001 — 2026-08-22

> Ciclo `/afm:evolve`. Alvo: `docs/` deste repo. Plugin (`framework-template/`) intocado (pós-condição de fronteira verde).
> Sintoma articulado que disparou: linha de status stale da regra 6 + sinais de saúde acumulados (SessionStart).

## Vital signs (antes → depois)

| Sinal | Antes | Depois |
| --- | --- | --- |
| Regra 6 status | "Compliant hoje (0 arquivos >300)" — **falso** | Reflete o scan real (2 de produção >300, tech-debt forward-only) |
| Gotcha por-path pra `t.procedure`/erro | ausente | presente (9 gatilhos, era 8) |
| Refs stale em `gotchas.md` | 2 (`verify.ts`, `post/revalidate/`) | 0 |
| Drift de versão (A3) | 3.1.0-rc.6 vs 3.5.0-rc.1 | **migrado → pinado em 3.5.0-rc.1** |

## Achados (Phase 1 — diagnose mecânico + reflect comportamental)

1. **A3 — drift de versão (HIGH).** Instância `3.1.0-rc.6`, plugin `3.5.0-rc.1`. Cruza o v4 inteiro (F0-F8, camada protocol, hardening de hooks, fronteira absoluta do plugin, telemetria de sessão). **Aprovado no gate → `/afm:update` executado.** Descoberta por grep no range: a maioria dos releases é consumer-noop (superfície install-side: hooks, MCP, skills, testes de repo); substituições `AUTONOMY=`/`core/rubrics/` já satisfeitas (grep=0). **Dois deltas genuínos materializados:**
   - **Camada protocol (3.3.0-rc.1, conteúdo atual do 3.5.0):** `docs/protocols/{p1,p2,p3}.md` copiados verbatim do plugin (já incluem `MAX_PARALLEL_SUBAGENTS` do 3.5.0) + `docs/_focus.md` criado com `protocol: [A DEFINIR]` (postura honesta — comportamento não muda até o dono declarar, #4).
   - **Regra 13 estendida (3.4.0-rc.7):** verificação ganha scan de prefixo de credencial (`sk-`/`ghp_`/`AIza`/`xox*-`/JWT) sobre `docs/sessions/` — contrapartida de versionar narrativa. Amplia cobertura, não estreita.
   - `.gitignore`/versionamento de `docs/sessions/` e o próprio cobrimento de sessões pela regra 17 já estavam prontos. `.afm-version` pinado `3.1.0-rc.6` → `3.5.0-rc.1` (migração completa, sem delta de fora).
2. **Staleness — regra 6 (MEDIUM).** O comando de Verificação, rodado em 2026-08-22, lista `src/server/models/post.ts` (416) e `src/server/features/post/schema.ts` (314) — nenhum desta feature. A linha afirmava "Compliant hoje (0 arquivos, scan 2026-07-04)". **Corrigido:** claim reescrito pra refletir o real, como tech-debt forward-only; o **comando não foi tocado** (editar gatilho de Verificação é balde (b) de risco — estreitaria a própria supervisão).
   - Sub-achado registrado, **não corrigido:** o comando exclui `*.test.*` por nome mas não dirs `__test__/`, então também lista `prismaModels.ts` (429). Fica anotado no doc como fora-de-escopo (regra mira código produtivo); mudar o comando exigiria decisão própria.
3. **Reflect — gotcha ausente (LOW, comportamental).** `t.procedure` direto pula `withDomainErrors` → 500 silencioso. Vivido nesta sessão (`refreshSession`, feature 024). **Adicionado** a `gotchas.md` com gatilho por-path (o hook `PreToolUse` passa a avisar). Retrieval-before-create: `grep` confirmou nenhum equivalente.
4. **A4 — refs stale em `gotchas.md` (LOW).** `verify.ts`→`verifyToken.ts` (exemplo, linha 93); `post/revalidate/`→`post/procedures/revalidate.ts`+`domain/revalidate.ts` (Ref, linha 33 — estrutura pré-ADR-0006). Alvos reais provados por FS/git. **Corrigidos.**

## Filtrados (negative-filter — não promovidos)

- **2 menções a `src/test/repositories/`** (afm.md): não são refs mortas — a própria frase diz "foram substituídos por prisma-mock (ADR-0011)". Memória histórica, preservada.
- **~30 `[A DEFINIR]`** (prd/ust/ach/afm): postura deliberada da adoção retroativa (persona, proposta de valor, thresholds RNF — projeto pessoal). Idade não é sinal pra inventar resposta (#4). → **1 pergunta-de-postura A2, staged** pro próximo ciclo (LOW, sem folga de cap consumida por HIGH/MED).

## Baldes

- **(a) auto-aplicável:** nenhum (nada era restaurativo-não-semântico sem tocar superfície de hook/Verificação).
- **(b) aprovados no gate:** itens 1, 2, 3, 4 (todos os 4 marcados pelo dono).
- **(c) diferido:** sub-achado do comando da regra 6 (anotado, não editado); pergunta-de-postura A2 (staged).

## Audit records

| Item | Superfície | Efeito esperado | Risco de regressão |
| --- | --- | --- | --- |
| 2 | `afm.md` regra 6 (status, não comando) | Doc para de afirmar conformidade falsa | Nenhum — comando intocado, só a prosa de status |
| 3 | `gotchas.md` (append) | Hook avisa quem cria procedure com `t.procedure` | Nenhum — append puro, formato preservado (9 gatilhos) |
| 4 | `gotchas.md` (2 refs in-place) | Refs apontam pro código real | Nenhum — update-in-place de ref existente |

## Pós-condições (Phase 4)

- Formato parseável: `**Gatilho:**` 8→9, preservado. ✓
- Fronteira plugin: `framework-template/` intocado desde T0 (repo git limpo). ✓
- Regra 17: nenhum doc colapsou (>50%). ✓

## Staged pro próximo ciclo

- Pergunta-de-postura A2 sobre os `[A DEFINIR]` (ainda deliberados?).
- Sub-achado do comando da regra 6 (`__test__/` não excluído) — se virar ruído.

## Handoff

Migração A3 (`/afm:update`) é o próximo passo aprovado. Reconcile observa a eficácia do gotcha novo nas próximas sessões (validação = comportamento futuro, sem held-out).
