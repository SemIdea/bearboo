---
mechanism: reflect
date: 2026-09-16
---

# Evolution 2026-09-16-003 — post-delivery `reflect` (feature 028)

> À-la-carte `/afm:reflect`, fired by `deliver` 4.D — the cadence (`REFLECT_EVERY=1`, default) closed a deliverable. Target: this repo's `docs/`. The plugin (`framework-template/`) stays untouched.
> Evidence: **behavioral** (this session's trajectory — the delivery of feature 028, 10 commits), not a static scan.

> **À-la-carte:** fill only the applicable sections (behavioral findings, self-critique, buckets, promotion). Before/after vital signs and cooldown belong to the `evolve` cycle. Leave them empty. Do not invent values.

## Vital signs (before → after)

N/A — à-la-carte `reflect` does not run the 7-axis scan. One static signal was corroborated this session: the gate (`docs/tools/gate/guardrails.sh`) is red on `develop` itself (`PLUGIN-38` at budget 0; `PLUGIN-40` at 27 vs the 16 measured at baseline) — see R03 and the suggestions.

## Findings (φ_b — behavioral signature)

| ID | Symptom (evidence: command + file:line / event) | Doc causal status | Mechanism | Sev | Addressable in the consumer doc? |
| --- | --- | --- | --- | --- | --- |
| R01 | The `deliver` resumability detector returned Phase 3.A for a `plan.md` whose § 6 carried a load-bearing decision in prose ("Open decision for review (bucket b…)"), with no `[NEEDS CLARIFICATION:]` marker; only the user's prior "Beleza" kept an unconfirmed decision from executing. Evidence: `ops/deliver.md` (detector greps the marker) vs `docs/features/028-health-check/plan.md` § 6 (prose); the detector ran this session. | Partly — the local `afm.md` body never states the marker convention for the delivery residue (no § 2.2 in the materialized body; § 2.1 lost the marker-vs-`[TBD]` note); the plugin's vocabulary is marker-centric while the `plan` op materializes bucket (b) as prose. | **Missing convention**: a load-bearing residue left for the gate has no executable expression the detector can grep. | MED (latent) | Yes — `afm.md § 2.1` (applied). The plugin half is promotion candidate P1. |
| R02 | The T004 commit (a red test — the planned RED commit) was rejected by Husky: `yarn lint:staged` → `.lintstagedrc.json` runs `vitest related` on staged `src/**/*.ts`. T004 and T005 merged into one commit (the § 2 loop commits after GREEN). Evidence: the rejected commit this session; `.husky/pre-commit` + `.lintstagedrc.json`; the `lint-staged` key in `package.json` is dead config. | No — no doc promised a red commit. The local gotcha `007` covered pre-push but claimed "there is no CI" (stale) and never mentioned pre-commit. | **Additive fact + stale doc**: the hook contract was under-documented, and the plugin's `tasks` template splits RED/GREEN into tasks that cannot be separate commits here. | LOW | Yes — gotcha `007` updated (applied). Plugin half: promotion candidate P2. |
| R03 | The gate on a clean `develop` worktree is red: `PLUGIN-38` 1 violation with budget 0 (the copied `core-rules/PLUGIN-38-parser-marker.sh` flags its own 2 Portuguese literals), `PLUGIN-40` 27 vs budget 16. I proved "no new findings" twice by diffing against a clean worktree (T001, T009). Evidence: `guardrails.sh` on a clean worktree vs `docs/tools/gate/ratchet.tsv`. | Partly — `ratchet.tsv` (measured 2026-09-16, evolve cycle) under-records the committed tree, so `PLUGIN-35`'s "the gate starts green" does not hold. No normative doc guided wrong. | **Baseline measured against a different tree state** + a plugin runner that self-flags when copied into `core-rules/`. | MED (instance health) | Not by a prose edit — a tooling decision (re-baseline vs fix the 11 refs). Suggestion for the human + promotion P3. |

### Observations (non-addressable / not earned)

- **O01 (§ 1.2 — the repeated manual analysis):** the same "diff the gate output against a clean HEAD worktree" analysis ran twice (T001, T009), because the ratchet compares counts per rule, not finding identity. A guardrail is *eligible* (2 occurrences) but not *earned*: the surface (`docs/tools/gate/`) exists, yet a new runner or procedure would add ceremony for a case `--baseline` already resolves. Recorded for the next cycle; no install.
- **O02 (host):** under opencode there is no `Stop` hook, so no session skeleton exists — § 5 step 1.5 is a no-op this session, and the promotion candidates live in this report instead of `docs/sessions/<...>` § `afm_findings`.
- **O03 (target adaptation):** the approved refinement named "§ 2.2", but the instance's materialized `afm.md` body (3.30.0 pin) has no § 2.2; the equivalent surface is § 2.1, where the line landed. Also a small sign that the local `afm.md` body lags the pinned core.

## Self-critique (mandatory rival hypothesis — "was it the doc, or was it me?")

- **R01** — "the doc was clear, I just did not follow it": the plugin `core/afm.md` states the marker semantics; the plan authored in a previous session wrote bucket (b) as prose anyway. But the delivery vocabulary never ties the gate residue to the marker, and the detector's grep makes that tie load-bearing. Net: **real gap** — addressed with a local line plus promotion P1.
- **R02** — no rule was involved; the friction was information missing from the local gotcha. No rule weakened. The gotcha is updated, not a new rule created.
- **R03** — "a violated rule is not a wrong rule": no rule was weakened; the problem is the ratchet's tolerance versus the committed tree. The correction is tooling, not a rule edit.

## Buckets

- **(a) restorative, auto-applicable:** none.
- **(b) load-bearing (gate — approved 2026-09-16):** R01 (`afm.md § 2.1` line) and R02 (gotcha `007` update). Both applied after the gate.
- **(c) deferred / human decision:** R03 → re-baseline or fix the extra refs; O01 → observe next cycle.

### 4-field audit record (per applied proposal)

| Proposal | Targeted pattern | Edited surface | Expected behavior effect | Regression risk / inventory to preserve |
| --- | --- | --- | --- | --- |
| R01 | bucket-(b) residue written as prose | `docs/afm.md § 2.1` (append, 1 paragraph) | a resumed `deliver` stops at the gate instead of executing an unconfirmed decision; `tasks` and the detector see the marker | the § 3 index parser shape is untouched (no new rule line); § 2.1 criteria unchanged |
| R02 | pre-commit rejects a red test; stale "no CI" claim | `docs/gotchas/007-*.md` (update-in-place) | an agent plans RED+GREEN as one commit and does not chase the dead `package.json` key | the pre-push risk paragraph and the `Ref:` research link are preserved; frontmatter keys kept (`type/description/load/fires_on/fires_when`) so the index still reads it |

## Suggested promotion (afm_findings — record, never apply: #7 / plugin boundary)

- **P1 — Plan/deliver marker vocabulary.** The `plan` op materializes bucket (b) as a prose "open decision", and `deliver`'s resumability detector keys on `[NEEDS CLARIFICATION:]`; a plan halted at the gate is invisible to a resumed `deliver`. Candidate fix: `plan` writes the marker for a bucket-(b) residue, or the detector reads a stable prose shape. Evidence: `ops/deliver.md` (resumability detector) vs `docs/features/028-health-check/plan.md` § 6; the detector ran this session and returned Phase 3.A.
- **P2 — `tasks` template vs a test-running pre-commit.** The template splits RED and GREEN into separate tasks; in a repo whose pre-commit runs related tests (here via `.lintstagedrc.json`), the RED task cannot own a commit. Candidate fix: a note in `tasks.md`/`deliver.md` that RED and GREEN may share one commit when the pre-commit requires green. Evidence: the rejected T004 commit this session.
- **P3 — A `core-rules/` runner self-flags when copied.** `docs/tools/gate/core-rules/PLUGIN-38-parser-marker.sh` contains 2 Portuguese literals by design (its pattern list), and the same tool flags it in the consumer, where no exclusion exists. Candidate fix: exclude `core-rules/*` in the parser-marker runner, or ship the exclusion. Evidence: `guardrails.sh` on clean `develop` → `PLUGIN-38 … 1 violation(s), budget 0`.

## Suggestions for the human (recommendation-first)

1. **[Recommended] Re-measure the gate baseline** (`bash docs/tools/gate/guardrails.sh --baseline`) or fix the 11 extra `PLUGIN-40` refs by hand — the migrated gate is red on `develop`, and `PLUGIN-35`'s "the gate starts green" does not hold. *If ignored:* every future run pays the manual diff paid twice today, and a real new finding hides among the 27.
2. **Run `/afm:promote` for P1–P3** in a session with the capture hook (Claude Code), or keep them in this report until the opencode host grows one.

## Handoff

- Reconcile (this delivery's additive side): gotcha `007` updated under the same gate as R02 — watch whether a future delivery still attempts a red commit.
- R01 validation: future behavior — if a later plan carries the marker, a resumed `deliver` halts at the gate instead of executing. No held-out set.
