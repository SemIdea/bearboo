---
mechanism: reflect
date: 2026-09-16
---

# Evolution 2026-09-16-001 — à-la-carte `reflect`

> À-la-carte `/afm:reflect` (not an `evolve` cycle). Target: this repo's `docs/`. The plugin (`framework-template/`) stays untouched.
> Trigger: the reflection **cadence** (`Stop` hook) — 3 deliverables / 53 tasks closed since the last cycle, with an empty `reflection:` in session `2026-08-31-0220-5d247c`.
> Evidence: **behavioral** (the trajectory of session `5d247c`), not a static scan.

> **À-la-carte:** fill only the applicable sections (behavioral findings, self-critique, buckets, promotion). Before/after vital signs and cooldown belong to the `evolve` cycle. Leave them empty. Do not invent values.

## Vital signs (before → after)

N/A — à-la-carte `reflect` does not run the 7-axis scan. The current static signals (A1: 3 dead refs; A3: drift `3.5.0-rc.1` < `3.12.2`; A5: 2 § 3 triggers on a nonexistent path) come from SessionStart / `diagnose` and are addressed by `/afm:update` — see R01 and Handoff.

## Findings (φ_b — behavioral signature)

| ID | Symptom (evidence: command + file:line / event) | Doc causal status | Mechanism | Sev | Addressable in the consumer doc? |
| --- | --- | --- | --- | --- | --- |
| R01 | Consulting rules 31/36 to plan observability, I looked for per-rule files under `docs/rules/` (as the SessionStart digest describes: "one file each under `docs/rules/`"). `ls docs/rules/` → *No such file or directory*; `find docs -iname '*rule*'` → empty. This instance keeps the rules inline in `docs/afm.md § 3` (`docs/afm.md:142`; afm.md never references `docs/rules/`). | **No** — the consumer doc (`afm.md § 3`) is correct and not implicated. The misleading structural claim lives in the **plugin-generated digest** (SessionStart), which describes the newer layout (plugin `3.12.2`). | **Version drift**: the plugin split the rules into `docs/rules/*.md` after 3.5; the instance (`3.5.0-rc.1`) never migrated, so a digest describing the target layout points at a path that does not exist here. | LOW | **No** — afm.md is correct; the fix is `/afm:update`, not a doc edit. |
| R02 | `http://localhost:3000` hardcoded in 3 email templates (`register.ts`, `resendVerificationEmail.ts`, `sendResetPasswordEmail.ts`), surfaced by an **owner correction** (a screenshot of the production reset email pointing at localhost). 3 occurrences + 1 correction → passes the rubric gate (`rubrics/failure-classification.md`: ≥2 or 1 correction). | No — no doc told me to hardcode it; a latent defect, not an instructiveness failure. | N/A doc. | LOW | Already remediated (`ctx.env.siteUrl` + regression tests in the 3 `__test__` files asserting `.not.toContain("localhost")`). |

### Observations (non-addressable findings — no proposal)

- **R01** behaviorally corroborates the static signals already reported at SessionStart (A3 drift; 2 § 3 triggers on a nonexistent path). `diagnose` gives the `c` (mechanical cause); `reflect` confirms the `q/m` — the drift is not cosmetic, it cost a real detour this session. It is addressable only via `/afm:update` (migrate the instance), outside the consumer's editable surface.
- **R01 — second hit, same mechanism.** I first wrote this report in Portuguese. Rule 37 (ASD-STE100 English for artifacts) is **not** in the instance's SessionStart rule list (1–36), yet the newer `PreToolUse` hook enforces it and caught the write. Same drift as R01: the instance's own rule digest lags behind the enforcing hooks. More weight for `/afm:update`.
- **R02**: a repeated defect, but the capture surface (the regression tests) already exists and catches a recurrence. **I do not install a guardrail** (a CI/husky grep) — it would be redundant with the tests plus human review, exactly the ceremony the owner already rejected (memory `feedback_prefer_written_rules_over_ceremony`; the coverage-gate rejection). The `ctx.env.siteUrl` pattern is now the copy example across the 3 files.

## Self-critique (mandatory rival hypothesis — "was it the doc, or was it me?")

- **R01** — "the doc was clear, I just did not follow it": partly (I could have opened `afm.md § 3` inline first). But the digest directed me literally to `docs/rules/`. Net: **real version drift**, already tracked statically; not a "wrong rule" and not purely my error. It stays an observation plus a suggestion (addressable only by `/afm:update`).
- **R02** — "a violated rule is not a wrong rule": N/A, no rule was involved. I weaken nothing. The remediation is complete; a guardrail is not *earned* given the existing test coverage plus the owner's preference.

## Buckets

- **(a) auto-applicable (restorative, non-semantic):** none.
- **(b) load-bearing (gate):** none — neither finding has a consumer-doc edit surface (afm.md is correct; the defect is already remediated). No edit, no gate (#8: a question must be *earned*).
- **(c) deferred / handoff:** R01 → `/afm:update` (suggestion for the human); R02 → observation, no action.

## Suggested promotion (`afm_findings` — record, never apply: #7 / plugin boundary)

- **From R01:** in an instance adopted by `refactor` and behind the watermark, the `SessionStart` digest describes a rule layout (`docs/rules/*.md`, one file per rule) that the instance **does not have**, which can misdirect an agent going to consult a rule by file. The same drift also drops rule 37 from the instance's rule list while the hooks still enforce it. Candidate plugin improvement (record-only): the digest should either (i) describe the instance's **actual** rule location, or (ii) flag when the instance is pre-split / behind the watermark. Evidence: session `5d247c`, `ls docs/rules/` → absent vs digest "one file each under docs/rules/"; rule 37 enforced by `PreToolUse` but absent from the 1–36 list.

## Suggestions for the human (recommendation-first)

1. **[Recommended] Run `/afm:update`** — the drift (`3.5.0-rc.1` → `3.12.2`) is not cosmetic: it cost a real detour this session (R01, twice) and it is what the vital signs already ask for. It materializes the rule split and clears the 2 dead § 3 triggers. *Consequence if ignored:* every future session that consults a rule by file repeats the detour, and the local rule list keeps lagging the enforced rules.
2. **R02 — no new mechanism.** The regression tests in the 3 files plus the `ctx.env.siteUrl` pattern already cover it. A gotcha only if you foresee many more email templates — I lean **no** (grow on demand, #5).

## Handoff

- Reconcile (separate from this reflect — additive drift, not a behavioral finding): `docs/roadmap.md` carries stale content from last session's en-US correction — the FTS dictionary says `portuguese` where it is now `english` (lines 15, 392), and the README is listed as "missing" although #230 shipped it (line 551). Fold it into the next PR that touches the roadmap, or a small reconcile.
- R01 validation: future behavior — if `/afm:update` runs, the detour disappears in later sessions (no held-out set).
