---
type: session
tier: dual
state: open
name: release 026–027, prod email fix, observability planning
date: 2026-08-31
claude_session_id: 5d247c66-a1f6-491d-86cb-2be83b1859e9
cwd: /home/codork/Documents/Github/bearboo
created_at: 2026-08-31T02:20:58Z
reflection: docs/evolution/2026-09-16-001.md
---

# Sessão 2026-08-31-0220 — docs--afm-log

> **Dois tiers no mesmo arquivo.** O bloco de **handoff** é episódico e single-use: `state: open|consumed` governa só ele (consumido pelo reconcile). O resto é **telemetria durável** — fica. Fora dos eixos A1–A7 do diagnose.
>
> Este esqueleto é 0-LLM (hook `Stop`). Timeline e `afm_findings` são preenchidos pelo `/afm:reflect` ao fim de um protocolo; sem reflect, a sessão fica só com o mecânico.

## files_touched

  - docs/.afm-log/events/2026-08.md

## summary (eventos desta sessão)

  - [2026-08-27T02:34:20Z] rule | s=5d247c | edit em docs/afm.md
  - [2026-08-27T02:34:58Z] feature-tasks | s=5d247c | edit em docs/features/027-native-fulltext-search/tasks.md
  - [2026-08-27T02:34:58Z] deliverable | s=5d247c | 027-native-fulltext-search
  - [2026-08-27T02:36:00Z] task | s=5d247c | commit
  - [2026-08-31T02:10:04Z] task | s=5d247c | commit
  - [2026-08-31T02:19:08Z] doc | s=5d247c | edit em docs/roadmap.md
  - [2026-08-31T02:19:12Z] doc | s=5d247c | edit em docs/roadmap.md
  - [2026-08-31T02:20:12Z] task | s=5d247c | commit

## o que foi pedido

  - Deliver two features via `/afm:deliver`: the integration test harness (026, Testcontainers + real Postgres), then native full-text search (027, tsvector/ts_rank).
  - Explain the design, not just ship it: why FTS is raw SQL, and why the search does two DB calls (rank-then-hydrate).
  - Debug the production DB migration confusion (a `db push` failure on the generated column, then a P3009 phantom migration).
  - **Mid-session correction that widened scope:** project content is en-US, not pt-BR — switch the FTS dictionary `portuguese` → `english` and update the language rule (afm.md § 1.3).
  - Ship the release: rewrite the README, open develop → main, fix the production password-reset email pointing at localhost, and merge with a merge commit.
  - This segment: report blog roadmap status and plan the next steps → the owner picked Observability (Phase 11: `/api/health` + request ID).

## como foi executado

  - `/afm:deliver` drove 026 and 027 end to end (discovery → gate → execute → reconcile). Both landed CI-green and merged.
  - Rules that mattered: rule 1 (test-first — the search tests moved to `*.integration.ts` because prisma-mock cannot run `tsvector`); rule 30 (no direct Prisma in domain/procedure); **rule 13 — refused to run a migration with a live prod credential the owner pasted, flagged rotation, and never wrote it to any file, command, or this log (redacted)**; rule 18 (ambient churn kept out of commits); rules 32/34 (PRs to develop, English titles).
  - Where AFM helped: the DI seam (`src/test/setup.ts`) let integration reuse the mock seam against real Postgres; the ADR-0027 pattern (generated `tsvector` + rank-then-hydrate + setweight) executed cleanly.
  - Where the harness got in the way (see `reflection`): looked for the hard rules under `docs/rules/` — absent, this instance keeps them inline in afm.md § 3 (version drift); rule 37 (English artifacts) is not in the instance's 1–36 list but the `PreToolUse` hook enforces it, so this report was authored in pt-BR first and redone. The Prisma generated-column gotcha bit twice (`db push` + P3009 phantom) — captured as a gotcha.
  - This segment ran no delivery op: a status read of `roadmap.md` + git, then one earned `AskUserQuestion` gate to pick the track.

## afm_findings

  - **finding:** In a `refactor`-adopted instance behind the watermark, the `SessionStart` digest describes a rule layout (`docs/rules/*.md`, one file per rule) the instance does not have, and drops rule 37 from the 1–36 list while the hooks still enforce it — both can misdirect the agent.
    **evidence:** session `5d247c`; `ls docs/rules/` → absent vs digest "one file each under docs/rules/"; rule 37 enforced by `PreToolUse` but absent from the injected list; instance `3.5.0-rc.1` < plugin `3.12.2`.
    **suggestion:** the digest should describe the instance's actual rule location, or flag a pre-split / behind-watermark instance. Record via `/afm:promote`; never apply (plugin boundary #7). Full write-up in `docs/evolution/2026-09-16-001.md`.

## open_questions

  - Will the owner run `/afm:update` to clear the version drift (`3.5.0-rc.1` → `3.12.2`)? It cost a real detour this session.
  - Standing owner actions that gate the real prod fixes: set `SITE_URL` in the production environment (else reset emails stay on localhost); rotate the pasted Prisma Postgres credential (compromised, in the transcript).

## next_steps

  - Observability (Phase 11), owner-approved track: T1 — `/api/health` (DB ping + version, 200/503) via an infra ping, keeping Prisma out of domain/procedure (rule 30); T2 — request ID threaded into the canonical log line (`createContext` + `EmitMeta` + `withCanonicalLog`).
  - Sync to `develop` first (still on `fix/email-links-use-site-url`) before branching. Fold the roadmap reconcile (FTS `english`; README done) into the T1 PR as a boy-scout.
