protocol: [A DEFINIR]

<!-- Slot-state pequeno e sobrescrivível (regra 20). A linha `protocol:` declara a
postura de autonomia vigente (presets em docs/protocols/{p1,p2,p3}.md; definição
canônica no core/protocol.md do plugin). `[A DEFINIR]` = defaults pré-v4 (comportamento
não muda até o dono declarar — presumir seria invenção, princípio #4). P1 exige
invocação explícita na sessão, nunca é concedido pelo repo. -->
