yarn lint

set +e
yarn lint-staged --diff "origin/main...HEAD"
STATUS=$?
set -e

if [ $STATUS -ne 0 ]; then
  echo "❌ Staged tests failed. Push aborted."
  exit 1
fi

echo "✅ Staged tests passed. Remember to run full suite locally before push."
